if (GSSAPI_FOUND AND SQLITE_FOUND)
  if (NOT SQLITE_VERSION VERSION_LESS "3.7.7")
    include_directories(${GSSAPI_INCS})
    include_directories(${SQLITE_INCLUDE_DIR})

    add_library(gssapiauth SHARED gssapi_auth.cc gssapi_auth_common.cc gssapi_backend_auth.cc)
    target_link_libraries(gssapiauth maxscale-common ${GSSAPI_LIBS} ${SQLITE_LIBRARIES} mariadbclient)
    set_target_properties(gssapiauth PROPERTIES VERSION "1.0.0" LINK_FLAGS -Wl,-z,defs)
    install_module(gssapiauth core)
  else()
    message(FATAL_ERROR "Minimum requires SQLite version for GSSAPIAuth is 3.7.7, current SQLite version is ${SQLITE_VERSION}")
  endif()
else()
  message(FATAL_ERROR "SQLite and GSSAPI/Kerberos libraries are required by GSSAPIAuth")
endif()
