add_library(kafkaconsumer SHARED kafkaconsumer.cc consumer.cc producer.cc table.cc config.cc)
add_dependencies(kafkaconsumer connector-c librdkafka)
set_target_properties(kafkaconsumer PROPERTIES VERSION "1.0.0"  LINK_FLAGS -Wl,-z,defs)
target_link_libraries(kafkaconsumer maxscale-common ${RDKAFKA_LIBRARIES})
install_module(kafkaconsumer core)
