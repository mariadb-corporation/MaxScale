select @@server_id; -- maxscale begin route to master:3000
select @@server_id; -- maxscale max_slave_replication_lag = 10:3000
select @@server_id; -- maxscale route to server server3:3002
select @@server_id; -- maxscale max_slave_replication_lag = 10:3000
select @@server_id; -- maxscale end:
select @@server_id; # maxscale begin route to master:3000
select @@server_id; # maxscale max_slave_replication_lag = 10:3000
select @@server_id; # maxscale route to server server3:3002
select @@server_id; # maxscale max_slave_replication_lag = 10:3000
select @@server_id; # maxscale end:
